function sample=readsample(address,classnum,num)
%??????????????????????
%??????address??????????????????????,classnum????????????????????,num??????????????
%??????????????
allsamples=[];
image=imread('face2.jpg');%??????????????
[rows cols]=size(image);%????????????????????
for i=classnum
    for j=num
        %a=imread(strcat(address,'face2','.jpg'));
        b=image(1:rows*cols);
        b=double(b);
        allsamples=[allsamples;b];
    end
end
sample=allsamples;